# Two-tower training on ESCI (relevant pairs only, in-batch negatives + self-adversarial reweighting)
data_dir: data
model_name: all-MiniLM-L12-v2
shared_tower: false
batch_size: 64
epochs: 3
lr: 1.0e-5
temperature: 0.2
reweight_hard: true
hard_weight_power: 2.0
# Partial-query augmentation (head/tail truncations) so model sees shorter queries
use_query_augment: true
augment_prob: 0.5
save_path: data/model.pt
# Task 1 reduced set (~48k queries, smaller corpus) for more tractable retrieval eval
small_version: false
